jQuery(function ($) {

    /*=============================================================
     Authour URI: www.binarytheme.com
     License: Commons Attribution 3.0
 
     http://creativecommons.org/licenses/by/3.0/
 
     100% To use For Personal And Commercial Use.
     IN EXCHANGE JUST GIVE US CREDITS AND TELL YOUR FRIENDS ABOUT US
    
     ========================================================  */

    /*==========================================
    GENERAL CUSTOM SCRIPTS
    =====================================================*/

    //  HEADER SLIDE /CAROUSEL SECTION FUNCTION 
	// Tiempo de cambio

/*
// Funcion de random
function randomToN(maxVal) {
   var randVal = Math.random() * maxVal;
   return typeof 0 == 'undefined' ? Math.round(randVal) : randVal.toFixed(0);
}
// Timer
var timer = setInterval(function(){
    // Seleciona una imagen de la lista
    var ram = img[parseFloat( randomToN(img.length) ) ];
    var img = ram == undefined || ram == null ? img[0] : ram;
    // Direcion donde estan la imagenes
    var dir = "assets/img/" + img;
    // Cambia la imagen
    $(".header").css("backgroundImage", "url(" + dir + ")");
}, time);

$("li:random").text()
*/
/*
setInterval(function(){myTimer()}, 8000);

function myTimer() {
    var time = 15000;
  
    // Imagenes en random
    var img = [ "bg.jpg", "bg2.jpg", "bg3.jpg" ];
    var random = img[Math.floor(Math.random()*img.length)]
    // Direcion donde estan la imagenes
    var dir = "assets/img/" + random;
    $(".header").css("backgroundImage", "url(" + dir + ")");
    
}
*/	
       
});